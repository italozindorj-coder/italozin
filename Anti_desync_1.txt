local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")
local LP = Players.LocalPlayer

-- // THEME (SAKURA BLOSSOM STYLE - LIGHT PINK + WHITE)
local BG         = Color3.fromRGB(255, 240, 245)   -- Very light pink/white base
local CARD_BG    = Color3.fromRGB(255, 255, 255)   -- Pure white cards
local CARD_HOV   = Color3.fromRGB(255, 230, 238)   -- Hover state (soft pink)
local BORDER     = Color3.fromRGB(255, 140, 180)   -- Sakura Pink
local BORDER2    = Color3.fromRGB(255, 100, 150)   -- Darker Sakura Pink
local WHITE      = Color3.fromRGB(255, 255, 255)   -- Pure White
local TEXT_PINK  = Color3.fromRGB(255, 100, 150)   -- Pink text for light backgrounds
local KB_BG      = Color3.fromRGB(255, 160, 190)   -- Button background

-- // STATE
local State = {
    autoBatToggled = false,
    hittingCooldown = false,
    guiVisible = true,
    mobileMode = false,
}

local Keys = {
    autoBat = Enum.KeyCode.X,
    autoBatType = "Keyboard", 
    guiHide = Enum.KeyCode.LeftControl,
}

local h, hrp = nil, nil

-- // FORMAT HELPER FOR CONTROLLER BUTTONS
local function formatKeyName(key)
    local name = key.Name
    if string.find(name, "Button") then
        return string.gsub(name, "Button", "")
    end
    return name
end

-- // CONFIG
local function saveConfig()
    local cfg = {
        autoBatKey = Keys.autoBat.Name,
        autoBatKeyType = Keys.autoBatType,
        mobileMode = State.mobileMode,
    }
    pcall(function() 
        writefile("SakuraAutoBatDesyncConfig.json", HttpService:JSONEncode(cfg)) 
    end)
end

local function loadConfig()
    local hasFile = false
    pcall(function() hasFile = isfile("SakuraAutoBatDesyncConfig.json") end)
    if not hasFile then return end
    
    local ok, cfg = pcall(function() 
        return HttpService:JSONDecode(readfile("SakuraAutoBatDesyncConfig.json")) 
    end)
    if not ok or not cfg then return end
    
    if cfg.autoBatKey and Enum.KeyCode[cfg.autoBatKey] then
        Keys.autoBat = Enum.KeyCode[cfg.autoBatKey]
    end
    
    if cfg.autoBatKeyType then
        Keys.autoBatType = cfg.autoBatKeyType
    end
    
    if cfg.mobileMode ~= nil then 
        State.mobileMode = cfg.mobileMode 
    end
end

loadConfig()

-- // CLEANUP OLD GUI
for _, name in pairs({"SakuraAutoBatDesyncGUI", "EnvyAutoBatDesyncGUI", "MwvaneNewaBatDesyncGUI", "PhazeAutoBatDesyncGUI"}) do
    local old = game:GetService("CoreGui"):FindFirstChild(name)
    if old then old:Destroy() end
end

-- // MAIN GUI
local gui = Instance.new("ScreenGui")
gui.Name="SakuraAutoBatDesyncGUI"; gui.ResetOnSpawn=false; gui.DisplayOrder=10
gui.IgnoreGuiInset=true; gui.Parent=LP:WaitForChild("PlayerGui")

-- // COOL BINDING OVERLAY (Pink and See-Through)
local bindOverlay = Instance.new("Frame", gui)
bindOverlay.Size = UDim2.new(1, 0, 1, 0)
bindOverlay.BackgroundColor3 = BORDER
bindOverlay.BackgroundTransparency = 1
bindOverlay.Visible = false
bindOverlay.ZIndex = 100

local bindBox = Instance.new("Frame", bindOverlay)
bindBox.Size = UDim2.new(0, 320, 0, 140)
bindBox.Position = UDim2.new(0.5, -160, 0.5, -70)
bindBox.BackgroundColor3 = BORDER2
bindBox.BackgroundTransparency = 1
bindBox.ZIndex = 101
Instance.new("UICorner", bindBox).CornerRadius = UDim.new(0, 10)
local bindBoxStroke = Instance.new("UIStroke", bindBox)
bindBoxStroke.Color = WHITE
bindBoxStroke.Thickness = 2
bindBoxStroke.Transparency = 1

local bindTitle = Instance.new("TextLabel", bindBox)
bindTitle.Size = UDim2.new(1, 0, 0, 40)
bindTitle.Position = UDim2.new(0, 0, 0, 10)
bindTitle.BackgroundTransparency = 1
bindTitle.Text = "WAITING FOR INPUT"
bindTitle.TextColor3 = WHITE
bindTitle.Font = Enum.Font.GothamBlack
bindTitle.TextSize = 20
bindTitle.TextTransparency = 1
bindTitle.ZIndex = 102

local bindDesc = Instance.new("TextLabel", bindBox)
bindDesc.Size = UDim2.new(1, 0, 0, 30)
bindDesc.Position = UDim2.new(0, 0, 0, 45)
bindDesc.BackgroundTransparency = 1
bindDesc.Text = "Press any Key or Controller Button...\n(Press ESC to cancel)"
bindDesc.TextColor3 = WHITE
bindDesc.Font = Enum.Font.GothamSemibold
bindDesc.TextSize = 13
bindDesc.TextTransparency = 1
bindDesc.ZIndex = 102

-- Mobile Cancel Button (Below Bind Screen)
local cancelBindBtn = Instance.new("TextButton", bindBox)
cancelBindBtn.Size = UDim2.new(0, 140, 0, 30)
cancelBindBtn.Position = UDim2.new(0.5, -70, 1, -40)
cancelBindBtn.BackgroundColor3 = CARD_BG
cancelBindBtn.Text = "CANCEL"
cancelBindBtn.TextColor3 = TEXT_PINK
cancelBindBtn.Font = Enum.Font.GothamBold
cancelBindBtn.TextSize = 12
cancelBindBtn.BackgroundTransparency = 1
cancelBindBtn.TextTransparency = 1
cancelBindBtn.ZIndex = 103
Instance.new("UICorner", cancelBindBtn).CornerRadius = UDim.new(0, 8)

-- // DRAGGABLE WINDOW
local main = Instance.new("Frame",gui)
main.Name="Main"; main.Size=UDim2.new(0,210,0,100); main.Position=UDim2.new(0.5,-105,0.5,-50)
main.BackgroundColor3=BG; main.BorderSizePixel=0; main.Active=true; main.ClipsDescendants=true
Instance.new("UICorner",main).CornerRadius=UDim.new(0,10)
local mainStroke = Instance.new("UIStroke",main); mainStroke.Color=BORDER; mainStroke.Thickness=2

do
    local dragging, dragInput, dragStart, mainStart = false, nil, nil, nil
    main.InputBegan:Connect(function(inp)
        if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then
            dragging=true; dragStart=inp.Position; mainStart=main.Position
            inp.Changed:Connect(function()
                if inp.UserInputState==Enum.UserInputState.End then dragging=false end
            end)
        end
    end)
    main.InputChanged:Connect(function(inp)
        if inp.UserInputType==Enum.UserInputType.MouseMovement or inp.UserInputType==Enum.UserInputType.Touch then dragInput=inp end
    end)
    UIS.InputChanged:Connect(function(inp)
        if inp==dragInput and dragging then
            local dx = inp.Position.X - dragStart.X
            local dy = inp.Position.Y - dragStart.Y
            main.Position = UDim2.new(mainStart.X.Scale, mainStart.X.Offset+dx, mainStart.Y.Scale, mainStart.Y.Offset+dy)
        end
    end)
end

-- // HEADER
local header = Instance.new("Frame",main)
header.Size=UDim2.new(1,0,0,35); header.BackgroundColor3=BORDER; header.BorderSizePixel=0; header.ZIndex=5
Instance.new("UICorner",header).CornerRadius=UDim.new(0,10)
local headerPatch = Instance.new("Frame",header)
headerPatch.Size=UDim2.new(1,0,0,10); headerPatch.Position=UDim2.new(0,0,1,-10)
headerPatch.BackgroundColor3=BORDER; headerPatch.BorderSizePixel=0; headerPatch.ZIndex=4

local headerDiv = Instance.new("Frame",header)
headerDiv.Size=UDim2.new(1,0,0,1); headerDiv.Position=UDim2.new(0,0,1,-1)
headerDiv.BackgroundColor3=BORDER2; headerDiv.BorderSizePixel=0; headerDiv.ZIndex=6

local titleLbl = Instance.new("TextLabel",header)
titleLbl.Size=UDim2.new(0,110,1,0); titleLbl.Position=UDim2.new(0,10,0,0)
titleLbl.BackgroundTransparency=1; titleLbl.Text="SAKURA ANTI DESYNC"
titleLbl.TextColor3=WHITE; titleLbl.Font=Enum.Font.GothamBlack; titleLbl.TextSize=10
titleLbl.TextXAlignment=Enum.TextXAlignment.Left; titleLbl.ZIndex=6

local titleStroke = Instance.new("UIStroke", titleLbl)
titleStroke.Color = BORDER2
titleStroke.Thickness = 1

-- Mobile/PC Toggle Button
local modeToggleBtn = Instance.new("TextButton", header)
modeToggleBtn.Size = UDim2.new(0, 42, 0, 20)
modeToggleBtn.Position = UDim2.new(1, -72, 0.5, -10)
modeToggleBtn.BackgroundColor3 = BORDER2
modeToggleBtn.BorderSizePixel = 0
modeToggleBtn.Text = State.mobileMode and "PC" or "MOBILE"
modeToggleBtn.TextColor3 = WHITE
modeToggleBtn.Font = Enum.Font.GothamBold
modeToggleBtn.TextSize = 8
modeToggleBtn.ZIndex = 10
Instance.new("UICorner", modeToggleBtn).CornerRadius = UDim.new(0, 5)

local closeBtn = Instance.new("TextButton",main)
closeBtn.Size=UDim2.new(0,20,0,20); closeBtn.Position=UDim2.new(1,-26,0,7)
closeBtn.BackgroundColor3=BORDER2; closeBtn.BorderSizePixel=0
closeBtn.Text="X"; closeBtn.TextColor3=WHITE; closeBtn.Font=Enum.Font.GothamBlack
closeBtn.TextSize=10; closeBtn.ZIndex=10
Instance.new("UICorner",closeBtn).CornerRadius=UDim.new(0,5)

closeBtn.MouseButton1Click:Connect(function()
    State.autoBatToggled = false
    gui:Destroy()
end)

-- // CONTENT AREA
local content = Instance.new("Frame",main)
content.Size=UDim2.new(1,-16,1,-45); content.Position=UDim2.new(0,8,0,40)
content.BackgroundTransparency=1; content.BorderSizePixel=0

-- // AUTO BAT TOGGLE ROW
local toggleRow = Instance.new("Frame",content)
toggleRow.Size=UDim2.new(1,0,0,26); toggleRow.BackgroundColor3=CARD_BG; toggleRow.BorderSizePixel=0
Instance.new("UICorner",toggleRow).CornerRadius=UDim.new(0,6)
local toggleRowStroke = Instance.new("UIStroke",toggleRow); toggleRowStroke.Color=BORDER
local toggleLbl = Instance.new("TextLabel",toggleRow)
toggleLbl.Size=UDim2.new(0,70,1,0); toggleLbl.Position=UDim2.new(0,8,0,0)
toggleLbl.BackgroundTransparency=1; toggleLbl.Text="Auto Bat"; toggleLbl.TextColor3=TEXT_PINK
toggleLbl.Font=Enum.Font.GothamBold; toggleLbl.TextSize=10; toggleLbl.TextXAlignment=Enum.TextXAlignment.Left

-- Keybind button
local keyBtn = Instance.new("TextButton",toggleRow)
keyBtn.Size=UDim2.new(0,40,0,18); keyBtn.Position=UDim2.new(1,-82,0.5,-9)
keyBtn.BackgroundColor3=KB_BG; keyBtn.BorderSizePixel=0; keyBtn.Text=formatKeyName(Keys.autoBat)
keyBtn.TextColor3=WHITE; keyBtn.Font=Enum.Font.GothamBold; keyBtn.TextSize=8; keyBtn.ZIndex=11
keyBtn.TextScaled = true
local keyBtnPadding = Instance.new("UIPadding", keyBtn)
keyBtnPadding.PaddingTop = UDim.new(0, 2); keyBtnPadding.PaddingBottom = UDim.new(0, 2)
Instance.new("UICorner",keyBtn).CornerRadius=UDim.new(0,4)
local ks = Instance.new("UIStroke",keyBtn); ks.Color=BORDER2; ks.Thickness=1
local kListening = false; local kConn

local function showBindOverlay(show)
    if show then
        bindOverlay.Visible = true
        TweenService:Create(bindOverlay, TweenInfo.new(0.3), {BackgroundTransparency = 0.5}):Play()
        TweenService:Create(bindBox, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {BackgroundTransparency = 0.2}):Play()
        TweenService:Create(bindBoxStroke, TweenInfo.new(0.3), {Transparency = 0.2}):Play()
        TweenService:Create(bindTitle, TweenInfo.new(0.3), {TextTransparency = 0}):Play()
        TweenService:Create(bindDesc, TweenInfo.new(0.3), {TextTransparency = 0}):Play()
        TweenService:Create(cancelBindBtn, TweenInfo.new(0.3), {BackgroundTransparency = 0, TextTransparency = 0}):Play()
    else
        local t1 = TweenService:Create(bindOverlay, TweenInfo.new(0.2), {BackgroundTransparency = 1})
        TweenService:Create(bindBox, TweenInfo.new(0.2), {BackgroundTransparency = 1}):Play()
        TweenService:Create(bindBoxStroke, TweenInfo.new(0.2), {Transparency = 1}):Play()
        TweenService:Create(bindTitle, TweenInfo.new(0.2), {TextTransparency = 1}):Play()
        TweenService:Create(bindDesc, TweenInfo.new(0.2), {TextTransparency = 1}):Play()
        TweenService:Create(cancelBindBtn, TweenInfo.new(0.2), {BackgroundTransparency = 1, TextTransparency = 1}):Play()
        t1:Play()
        t1.Completed:Connect(function() bindOverlay.Visible = false end)
    end
end

local function stopListen(key)
    kListening = false
    if kConn then 
        kConn:Disconnect()
        kConn = nil 
    end
    
    showBindOverlay(false)
    TweenService:Create(ks, TweenInfo.new(0.1), {Color = BORDER2}):Play()
    
    if key then 
        keyBtn.Text = formatKeyName(key)
        Keys.autoBat = key
        saveConfig()
    end
end

cancelBindBtn.MouseButton1Click:Connect(function()
    if kListening then stopListen(nil) end
end)

keyBtn.MouseButton1Click:Connect(function()
    if kListening then return end
    
    kListening = true
    TweenService:Create(ks, TweenInfo.new(0.1), {Color = WHITE}):Play()
    
    showBindOverlay(true)
    task.wait(0.1) 
    
    kConn = UIS.InputBegan:Connect(function(inp)
        if not kListening then return end
        
        if inp.UserInputType == Enum.UserInputType.Keyboard then
            if inp.KeyCode == Enum.KeyCode.Escape then 
                stopListen(nil)
                return 
            end
            if inp.KeyCode ~= Enum.KeyCode.Unknown then
                Keys.autoBatType = "Keyboard"
                stopListen(inp.KeyCode)
            end
        elseif inp.UserInputType.Name:find("Gamepad") then
            if inp.KeyCode ~= Enum.KeyCode.Unknown then
                Keys.autoBatType = "Gamepad"
                stopListen(inp.KeyCode)
            end
        end
    end)
end)

-- Toggle pill
local pillBg = Instance.new("Frame",toggleRow)
pillBg.Size=UDim2.new(0,32,0,16); pillBg.Position=UDim2.new(1,-38,0.5,-8)
pillBg.BackgroundColor3=BG; pillBg.BorderSizePixel=0; pillBg.ZIndex=8
Instance.new("UICorner",pillBg).CornerRadius=UDim.new(0,8)
local pStroke = Instance.new("UIStroke",pillBg); pStroke.Color=BORDER; pStroke.Thickness=1
local dot = Instance.new("Frame",pillBg)
dot.Size=UDim2.new(0,10,0,10); dot.Position=UDim2.new(0,3,0.5,-5)
dot.BackgroundColor3=BORDER; dot.BorderSizePixel=0; dot.ZIndex=9
Instance.new("UICorner",dot).CornerRadius=UDim.new(0,5)

-- Mobile button (full width)
local mobileBtn = Instance.new("TextButton", toggleRow)
mobileBtn.Size = UDim2.new(1, -8, 1, -4)
mobileBtn.Position = UDim2.new(0, 4, 0, 2)
mobileBtn.BackgroundColor3 = BORDER
mobileBtn.BorderSizePixel = 0
mobileBtn.Text = "AUTO BAT"
mobileBtn.TextColor3 = WHITE
mobileBtn.Font = Enum.Font.GothamBold
mobileBtn.TextSize = 10
mobileBtn.Visible = State.mobileMode
mobileBtn.ZIndex = 12
Instance.new("UICorner", mobileBtn).CornerRadius = UDim.new(0, 4)
local mobileBtnStroke = Instance.new("UIStroke", mobileBtn)
mobileBtnStroke.Color = BORDER2
mobileBtnStroke.Thickness = 1

local function setToggleVisual(on)
    TweenService:Create(pillBg,TweenInfo.new(0.18),{BackgroundColor3=on and BORDER or BG}):Play()
    TweenService:Create(pStroke,TweenInfo.new(0.18),{Color=on and BORDER2 or BORDER}):Play()
    TweenService:Create(dot,TweenInfo.new(0.18,Enum.EasingStyle.Back),{
        Position=on and UDim2.new(1,-13,0.5,-5) or UDim2.new(0,3,0.5,-5),
        BackgroundColor3=on and WHITE or BORDER
    }):Play()
    
    TweenService:Create(mobileBtn,TweenInfo.new(0.18),{BackgroundColor3=on and BORDER2 or BORDER}):Play()
    mobileBtn.TextColor3 = WHITE
end

local function updateMode()
    if State.mobileMode then
        toggleLbl.Visible = false
        keyBtn.Visible = false
        pillBg.Visible = false
        mobileBtn.Visible = true
        modeToggleBtn.Text = "PC"
    else
        toggleLbl.Visible = true
        keyBtn.Visible = true
        pillBg.Visible = true
        mobileBtn.Visible = false
        modeToggleBtn.Text = "MOBILE"
    end
end

updateMode()

modeToggleBtn.MouseButton1Click:Connect(function()
    State.mobileMode = not State.mobileMode
    updateMode()
    saveConfig()
end)

mobileBtn.MouseButton1Click:Connect(function()
    State.autoBatToggled = not State.autoBatToggled
    setToggleVisual(State.autoBatToggled)
end)

local toggleClk = Instance.new("TextButton",toggleRow)
toggleClk.Size=UDim2.new(1,0,1,0); toggleClk.BackgroundTransparency=1; toggleClk.Text=""; toggleClk.ZIndex=6
toggleClk.MouseButton1Click:Connect(function()
    if State.mobileMode then return end
    State.autoBatToggled=not State.autoBatToggled
    setToggleVisual(State.autoBatToggled)
end)
toggleClk.MouseEnter:Connect(function() 
    if not State.mobileMode then TweenService:Create(toggleRow,TweenInfo.new(0.1),{BackgroundColor3=CARD_HOV}):Play() end
end)
toggleClk.MouseLeave:Connect(function() 
    if not State.mobileMode then TweenService:Create(toggleRow,TweenInfo.new(0.1),{BackgroundColor3=CARD_BG}):Play() end
end)

-- // LESS SHINE DISCORD LINK BUTTON
local discordBtn = Instance.new("TextButton", content)
discordBtn.Size = UDim2.new(1, 0, 0, 22)
discordBtn.Position = UDim2.new(0, 0, 0, 31)
discordBtn.BackgroundColor3 = CARD_BG
discordBtn.BorderSizePixel = 0
discordBtn.Text = "discord.gg/WKQ2SawcEB"
discordBtn.TextColor3 = TEXT_PINK
discordBtn.Font = Enum.Font.GothamSemibold
discordBtn.TextSize = 9
discordBtn.ZIndex = 10
Instance.new("UICorner", discordBtn).CornerRadius = UDim.new(0, 5)

discordBtn.MouseEnter:Connect(function()
    TweenService:Create(discordBtn,TweenInfo.new(0.15),{BackgroundColor3=CARD_HOV}):Play() 
end)
discordBtn.MouseLeave:Connect(function()
    TweenService:Create(discordBtn,TweenInfo.new(0.15),{BackgroundColor3=CARD_BG}):Play() 
end)
discordBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard("https://discord.gg/WKQ2SawcEB")
        discordBtn.Text = "Copied to clipboard!"
        task.delay(1.5, function()
            discordBtn.Text = "discord.gg/WKQ2SawcEB"
        end)
    end
end)

-- // BAT LOGIC
local function getBat()
    local char=LP.Character; if not char then return nil end
    local tool=char:FindFirstChild("Bat"); if tool then return tool end
    local bp2=LP:FindFirstChild("Backpack")
    if bp2 then tool=bp2:FindFirstChild("Bat"); if tool then tool.Parent=char; return tool end end
    return nil
end

local function tryHitBat()
    if State.hittingCooldown then return end; State.hittingCooldown=true
    pcall(function()
        local bat=getBat(); if bat then
            bat:Activate(); local ev=bat:FindFirstChildWhichIsA("RemoteEvent")
            if ev then ev:FireServer() end
        end
    end)
    task.delay(0.08, function() State.hittingCooldown=false end)
end

local function getClosestPlayer()
    if not hrp then return nil,math.huge end
    local cp,cd=nil,math.huge
    for _,p in pairs(Players:GetPlayers()) do
        if p~=LP and p.Character then
            local tr=p.Character:FindFirstChild("HumanoidRootPart")
            if tr then local d=(hrp.Position-tr.Position).Magnitude; if d<cd then cd=d; cp=p end end
        end
    end
    return cp,cd
end

-- // CHARACTER SETUP
local function setupChar(char)
    task.wait(0.1)
    h=char:WaitForChild("Humanoid",5); hrp=char:WaitForChild("HumanoidRootPart",5)
end

LP.CharacterAdded:Connect(setupChar)
if LP.Character then task.spawn(function() setupChar(LP.Character) end) end

-- // BAT AIMBOT HEARTBEAT
RunService.Heartbeat:Connect(function()
    if not (State.autoBatToggled and h and hrp) then return end
    local target,dist=getClosestPlayer()
    if target and target.Character then
        local tr=target.Character:FindFirstChild("HumanoidRootPart")
        if tr then
            if sethiddenproperty then sethiddenproperty(hrp, "PhysicsRepRootPart", tr) end
            local targetPos = tr.Position + Vector3.new(0, 0.9, 0)
            if (hrp.Position - targetPos).Magnitude > 8 then
                hrp.CFrame = CFrame.new(targetPos)
            end
            local cam = workspace.CurrentCamera
            cam.CFrame = CFrame.new(cam.CFrame.Position, tr.Position)
            tryHitBat()
        end
    end
end)

-- // KEYBIND HANDLER
UIS.InputBegan:Connect(function(inp, gp)
    if State.mobileMode then return end
    if gp then return end
    
    if Keys.autoBatType == "Keyboard" and inp.UserInputType == Enum.UserInputType.Keyboard then
        if inp.KeyCode == Keys.autoBat then
            State.autoBatToggled = not State.autoBatToggled
            setToggleVisual(State.autoBatToggled)
        elseif inp.KeyCode == Keys.guiHide then
            State.guiVisible = not State.guiVisible
            main.Visible = State.guiVisible
        end
    elseif Keys.autoBatType == "Gamepad" and inp.UserInputType.Name:find("Gamepad") then
        if inp.KeyCode == Keys.autoBat then
            State.autoBatToggled = not State.autoBatToggled
            setToggleVisual(State.autoBatToggled)
        end
    end
end)

print("[Sakura Anti Desync] Loaded successfully!")